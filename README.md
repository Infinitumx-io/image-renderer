# Infinitum X Image Renderer

## Usage

`docker-compose up`

## Speeding up

`docker-compose up --scale getData=x` change x to the number of replicas you want to run in parallel to get all the data faster fron the contract
